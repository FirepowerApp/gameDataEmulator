apiVersion: v1
kind: Service
metadata:
  name: gamedataemulator
spec:
  selector:
    app: gamedataemulator
  ports:
    - name: pbp
      port: 8125
      targetPort: pbp
    - name: stats
      port: 8124
      targetPort: stats
